
public interface Edible {

	// Can have fields, but they are all static
	
	// All methods are public, instance, abstract, and blank methods
		// No static methods
	
	// No need to write public, because assumed public
	
	public String howToEat();

	
}
